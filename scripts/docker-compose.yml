services:
  portfolio:
    container_name: portfolio
    image: node:latest
    volumes:
      - ../app:/app
    working_dir: /app
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
